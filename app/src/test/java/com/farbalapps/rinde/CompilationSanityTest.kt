package com.farbalapps.rinde

import org.junit.Test
import org.junit.Assert.*

/**
 * Basic sanity test to verify that the project structure and dependencies 
 * are correctly configured and the project can be compiled.
 */
class CompilationSanityTest {
    @Test
    fun build_isFunctional() {
        // If this test runs, the compilation of the test source set was successful
        assertTrue("Project compiled and test executed successfully", true)
    }
}
